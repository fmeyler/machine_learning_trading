from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This is an attempt to see if one can use machine learning to select assets that performs better than S&P500 during a given period',
    author='Fred',
    license='',
)
